#pragma once

namespace ml
{
    namespace core
    {
        namespace linalg
        {
            template<typename T, std::size_t size>
            class vector;
            template<typename T, std::size_t height, std::size_t width>
            class matrix;
            template<typename T, std::size_t size>
            class sparse_vector;
            template<typename T, std::size_t height, std::size_t width>
            class sparse_matrix;
        }
    }
}
